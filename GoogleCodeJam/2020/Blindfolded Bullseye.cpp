#include<bits/stdc++.h>
using namespace std;
char s[10];
int main()
{
    int T,A,B;
    scanf("%d %d %d",&T,&A,&B);
    while(T--)
    {
        for(int i=-100;i<=100;i++)
        {
            printf("%d %d\n",i,0),fflush(stdout),scanf(" %s",s);
            if(s[0]=='H')
            {
                for(int )
            }
        }
    }
    return 0;
}
